# Public Markets Domain

This domain teaches the Intelligence Engine public-market vocabulary.

The engine remains domain-independent. Domain-specific language belongs here, not in `app/models/engine` or `app/services/engine`.
